# 精准 SRT 问题验证计划

更新时间：2026-09-09

本文记录精准 SRT 已发现问题的验证路径和当前执行状态。统一未完成事项见《[精准 SRT 未完成事项总表](精准SRT未完成事项总表.md)》。

## 当前状态

| 事项 | 当前状态 | 说明 |
|------|----------|------|
| 有文案模式模型切换造成进度跳回 | 已定位，专项修复记录已完成 | 根因是 fallback 从头处理完整音频；是否通过当前版本需重新回归确认 |
| 有文案模式文案切分质量 | 部分完成 | 已使用 Sudachi 词边界；真实样本边界指标仍有未达标项 |
| 中文语义基准辅助日语 | 未开始 | 尚无中日对齐数据和人工语义边界 |
| CUDA 日文边界模型 | 实验基线 | 已训练，未接入生产，F1 为 0.606 |
| Sudachi 改造后的完整回归 | 待执行 | 需要重新运行 Python、core、类型检查、构建和真实音频验收 |
| RTX 2060 6GB | 待外部实机 | 当前仅有 RTX 5070 Ti 证据 |
| 四语快速语速质量门槛 | 未全部通过 | 已有历史运行数据，但不能用平均值覆盖未达标语言 |
| 增量 fallback | 未实现 | 当前 fallback 仍可能重新处理完整音频 |

## 已完成的复现与定位

原问题是有文案模式中 turbo 对齐不完整时触发 `PRECISE_SRT_ALIGNMENT_INCOMPLETE`，上层切换到 Large v3 并从头处理整个音频。用户看到的“中间空白”是进度重新开始造成的视觉跳回，最终 SRT 并非丢失内容。

相关代码入口：

- Python 正文锁定：`python/src/koubox_runtime/precise_srt.py::_ensure_mode_a_preserves_source`
- 日文失败后的 Sudachi 重对齐：`python/src/koubox_runtime/precise_srt_worker.py`
- 上层模型执行计划：`packages/core/src/tasks.ts::performPreciseSrt`

当前实现已经移除了旧的 `_mode_a_near_match` 和按字符权重重分配逻辑。日文对齐失败时先使用 Sudachi tokenized realign；仍失败时再按上层模型执行计划处理。旧文档中“放宽 near-match 阈值”的方案属于历史记录，不是当前实现说明。

## 尚未完成的验证

### 1. Sudachi 改造后自动化回归

```powershell
python\.venv\Scripts\python.exe -m unittest discover -s python\tests -p "test_*.py" -v
pnpm test
pnpm typecheck
pnpm build
git diff --check
```

通过条件：命令退出状态均为 0；实际测试数量、原始输出和失败信息写入《精准 SRT 模式 A/B 架构与验收》和 `VERIFICATION.txt`。

### 2. 日文真实音频回归

至少覆盖已有 10 条真实样本中的可用样本，分别验证模式 A、模式 B off 和模式 B auto：

- 模式 A 正文与用户文案展示归一化后 100% 一致；
- 时间轴正时长、严格递增、最长 3 秒；
- 每条字幕不超过 14 个有效字符；
- 日文不在 Sudachi 合法边界之外切分；
- 助词、助动词、接尾辞和接头辞的附着符合规则；
- 记录模型、fallback、耗时、显存和最终 SRT 哈希。

真实音频路径和文案路径必须在实际执行时记录，历史临时目录已清理，不能把历史目录写成当前存在的输入。

### 3. 日文未达标样本人工复核

已有历史问题：模式 A 样本 02、09 的边界召回低于 85%；样本 07、09 的条数差超过 10%；模式 B 样本 07 的边界召回为 68.09%、条数差为 20.83%。

处理顺序：

1. 人工试听并查看参考 SRT；
2. 判定参考边界是否符合自然口播和日语词边界；
3. 把确认后的结果写入人工金标准；
4. 再决定修改算法还是修改验收门槛；
5. 重新运行并记录前后差异。

### 4. 中文辅助基准

建立中日对齐样本，记录中文自然短语边界和对应的日语词区间。中文边界只作为语义辅助特征，最终切点必须落在 Sudachi 合法边界，并由日语人工标注确认。

最小验证指标：

- 中日片段对齐成功率；
- 映射到合法日语边界的比例；
- 映射偏移分布；
- 中文辅助前后日语边界 F1 变化；
- 复合词误切分率和助词孤立率。

### 5. CUDA 模型离线对比和生产接入

模型接入前先离线比较以下四种策略：

```text
Sudachi 规则
Sudachi + CUDA 模型
Sudachi + 中文辅助信号
现有生产动态规划
```

只有人工金标准测试集显示稳定改善，并且正文完整性、词内不切分率和 SRT 合法率不下降，才进入生产调用点。模型权重缺失、CUDA 不可用或模型推理异常时必须产生明确错误记录。

### 6. 四语快速语速门槛

重新按语言和速度统计 FLEURS test 的 off/auto：

- 1.00x：auto 相比 off 不得出现超过门槛的退化；
- 1.25x、1.50x：分别统计平均 CER/WER 改善；
- 候选至少两路一致，并满足置信度提升门槛；
- 变速音频时间戳不能直接进入最终 SRT；
- 未达标语言必须单独列出。

### 7. RTX 2060 6GB

在目标实机执行代表性日文和四语任务，保留原始 GPU 信息、显存峰值、退出状态、SRT 结构和显存释放结果。RTX 5070 Ti 结果仅作为参考，不能代替目标设备验收。

### 8. 增量 fallback

定义失败区间、上下文扩展、局部重新对齐、合并去重和进度计算规则。使用真实触发 `PRECISE_SRT_ALIGNMENT_INCOMPLETE` 的样本证明只重试问题区间，再修改 UI 阶段显示。

## 历史方案状态

| 历史方案 | 当前结论 |
|----------|----------|
| 放宽 `_mode_a_near_match` | 已被当前完全正文校验和 Sudachi 重对齐路径取代 |
| 有文案模式强制 Large v3 | 保留为可选对照方案，当前未落地 |
| 按字符长度权重重分配文案 | 已移除，不作为日文质量方案 |
| 单路 0.80 变速候选 | 已废弃，不作为发布证据 |
| Janome 日文分词 | 已被 SudachiPy + SudachiDict-core 取代 |
