# 精准 SRT 发布产物与测试数据清理记录

日期：2026-08-31

本次清理用于用户手动重新打包与重新测试。删除范围只包含旧便携发布产物、生成式验收数据、事务临时缓存和本轮专用评测脚本；产品源码、精准 SRT Python 模块、自动化单元测试源码、正式打包脚本和浏览器准备脚本保留。

## 已删除

- `apps/desktop/release`
- `apps/desktop/release-stale-20260831`
- `tests/precise-srt-validation`
- `tests/verification`
- `.tmp/node-compile-cache`
- `.tmp/torchinductor_Administrator`
- `.tmp/cmn_test.tsv`
- 本轮 FLEURS/精准 SRT 专用脚本：
  - `scripts/augment_fleurs_metrics.py`
  - `scripts/calibrate_precise_srt_speech_rate.py`
  - `scripts/download_fleurs_subset.py`
  - `scripts/evaluate_precise_srt_request.py`
  - `scripts/prepare_fleurs_speed_variants.py`
  - `scripts/run_fleurs_benchmark.py`
  - `scripts/test-precise-srt-blackbox.ps1`
  - `scripts/pack-portable-no-models.ps1`

## 保留

- `scripts/pack-portable.ps1`：正式便携包打包入口。
- `scripts/prepare-playwright-browsers.mjs`：正式构建依赖准备脚本。
- `scripts/*sha256*`、`scripts/make-sha256*`：发布包校验工具。
- `packages/core/test`、`python/tests`：产品自动化测试源码，用于重新打包前回归，不是生成式测试产物。
- `.tmp/transaction-20260831`：本轮修改的回滚与验证证据，保留以支持回退和审计。

## 清理后验证

清理后必须确认：

1. `apps/desktop/release` 和 `apps/desktop/release-stale-20260831` 不存在；
2. `tests/precise-srt-validation` 和 `tests/verification` 不存在；
3. `.tmp` 只剩 `transaction-20260831`；
4. `scripts` 只剩正式打包、浏览器准备、SHA256 校验脚本；
5. 产品源码与自动化测试源码仍可读取。
