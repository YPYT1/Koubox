---
description: 用户要求提交/推送时，用中文写提交信息并按仓库约定安全提交推送
alwaysApply: true
---

# 提交与推送（给 AI）

用户明确要求「提交」「推送」「commit」「push」时按本规则执行；未要求则不要主动提交或推送。

## 流程

1. 并行查看：`git status`、`git diff`（含暂存区）、`git log` 近期提交风格、分支跟踪与远程状态。
2. 根据变更写提交信息，执行 `git add` → `git commit` →（若要求推送）`git push`。
3. 提交后跑 `git status` 确认成功；推送后确认分支已跟踪远程。

## 提交信息（必须中文）

- 使用简体中文，1～2 句，说明「为什么改」，不要堆文件清单。
- 语气与近期 `git log` 一致（本仓库多为完整中文句，少用纯英文 feat/fix 前缀，除非历史已是该风格）。
- 示例：`新增视频下载工具：默认公开解析多平台链接，失败后再用登录态兜底。`

## 暂存范围

只暂存与本次功能相关的源码、锁文件、必要资源与测试。

**不要提交：**

- `node_modules/`、`out/`、构建产物、本地缓存
- 密钥与环境文件（`.env`、credentials 等）；若用户点名要提交须先警告
- 临时验证目录（如 `tests/verification/**` 里的下载视频、rollback 副本）
- 个人测试链接清单、本地草稿（如根目录 `视频链接`）

若用户明确要求带上验证产物，再单独加入对应小文件（如 `VERIFICATION.txt`），仍排除大体积二进制。

## 推送

- 仅在用户要求推送时执行。
- 当前分支无上游时用：`git push -u origin HEAD`。
- 禁止改 git config；禁止 `--force` 推 main/master；禁止跳过 hook（除非用户明确要求）。
- 推送完成后用中文简要回报：分支名、提交哈希与说明、远程跟踪状态；有未跟踪杂项可一句带过。
