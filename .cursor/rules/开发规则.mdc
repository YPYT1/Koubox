---
description: Koubox 简洁代码开发规则，约束 AI 最小改动、禁止兜底与过度抽象
alwaysApply: true
---

# 开发规则（给 AI）

1. 禁止写各种兜底代码；错误直接报错，不 silent fallback、不吞异常、不用默认值掩盖失败。
2. 只做当前任务要求的最小改动；禁止顺手重构、加功能、加抽象、加文档/测试（除非明确要求）。
3. 禁止为「将来可能」提前设计；一个调用点直接写，第二次复用再抽。
4. 禁止空壳包装（Manager/Helper/Util/Service）；优先短函数与直线逻辑。
5. 禁止废话注释；命名要自解释，禁用 data/temp/util/handler 等模糊名。
6. 保持现有代码风格与目录结构；能少改就少改。
7. 需求不清时先问，不要猜分支全写上。
